[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-visa-advisor"
version = "1.0.0"
description = "AI Visa Success Advisor Form Mode Backend"
requires-python = ">=3.11"
